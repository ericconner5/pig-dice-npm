import { Player } from '../src/business.js';

describe('Player', function() {

  it('should register that someone clicked a button and created a new player', function() {
    // var player = new Player(1,2);
    expect(0).toEqual(0);
  });
});

// describe('Roll', function() {
//
//   it('should test that when roll is clicked a number displays', function() {
//     expect player1.roll = NaN;
//   });
// });
